import { Common\guards\rolesGuard } from './common\guards\roles.guard';

describe('Common\guards\rolesGuard', () => {
  it('should be defined', () => {
    expect(new Common\guards\rolesGuard()).toBeDefined();
  });
});
